<!-- MAIN ADMIN -->
<section class="container">
    <div class="info">
        <?php
            if (isset($data['infos'])) {
                echo $data['infos'];
            }
        ?>
    </div>
    <div class="container-add">
        <a class="btn-add" href="/public/admin/add">Ajouter un article</a>
    </div>
    <table class="table-admin">
        <caption>Gérer les articles</caption>
        <thead id="thead-admin">
            <tr id="tr-admin">
                <th>Titre</th>
                <th>Supprimer l'article</th>
                <th>Modifier l'article</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($data['posts'] as $key => $post) : ?>
                <tr id="tr-admin">                
                    <td data-label="Titre" class="td-admin"><?= $post['title']; ?></td>
                    <td data-label="Supprimer l'article" class="td-admin">
                        <form method="post">
                            <input type="hidden" name="postid" value="<?= $post['id']; ?>">
                            <input class="btn-admin-red" type="submit" name="deletePostForm" value="Supprimer article">
                        </form>
                    </td>
                </tr>
            <?php endforeach ?>
        </tbody>
    </table>
</section>