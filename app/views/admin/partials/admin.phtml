<!-- MAIN ADMIN -->
<section class="container">
    <div class="info">
        <?php
            if (isset($data['infos'])) {
                echo $data['infos'];
            }
        ?>
    </div>
    <div class="container-add">
        <a class="btn-add" href="?page=admin&subpage=add">Add Article</a>
    </div>
    <table class="table-admin">
        <caption>Manage articles</caption>
        <thead id="thead-admin">
            <tr id="tr-admin">
                <th>Title</th>
                <th>Delete Articles</th>
                <th>Update Articles</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($data['posts'] as $key => $post) : ?>
                <tr id="tr-admin">                
                    <td data-label="Titre" class="td-admin"><?= $post['title']; ?></td>
                    <td data-label="Supprimer l'article" class="td-admin">
                        <form method="post">
                            <input type="hidden" name="postid" value="<?= $post['id']; ?>">
                            <input class="btn-admin-red" type="submit" name="deletePostForm" value="Delete Post">
                        </form>
                    </td>
                    <td data-label="Modifier l'article" class="td-admin">
                        <a class="btn-admin" href="?page=admin&subpage=update&id=<?= $post['id']; ?>">Update</a>
                    </td>
                </tr>
            <?php endforeach ?>
        </tbody>
    </table>
</section>