<main class="container">
    <h2>APEX Characters</h2>

    <?php foreach ($data as $post) : ?>  

        <article class="container-article">
            <div class="card">
                <h3><?= $post['title']; ?></h3>
                <div class="container-img">
                    <img src="assets/front/img/<?= $post['img'] ?>">
                </div>
                <div><?= nl2br(html_entity_decode($post['content'])); ?></div>
            </div>
        </article>

    <?php endforeach; ?>

</main>