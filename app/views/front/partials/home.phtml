<h1>home</h1>

<?php foreach ($data as $post) : ?>
    
    <div>
        <div><?= $post['title']; ?></div>
        <div><?= nl2br(html_entity_decode($post['content'])); ?></div>
    </div>

<?php endforeach; ?>